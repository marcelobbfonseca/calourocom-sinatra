# run in comand line
    $ irb
    > require './app'


# Run localhost application
    $ bundle exec rackup -p 3000
or
    $ ruby myapp.rb
    $ APP_ENV=production ruby my_app.rb
# Run tests
    $ rake test